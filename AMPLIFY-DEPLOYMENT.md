# 🚀 GitHub + AWS Amplify 部署指南

## 📋 前置準備

- ✅ GitHub 帳號
- ✅ AWS 帳號
- ✅ Git 已安裝
- ✅ 專案已完成並測試通過

## 🎯 部署步驟

### 步驟 1: 初始化 Git 倉庫

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: AWS 政府補助小助手 with Bedrock Agent"
```

### 步驟 2: 在 GitHub 創建倉庫

1. **登入 GitHub**: https://github.com
2. **點擊右上角 "+" → "New repository"**
3. **填寫倉庫資訊**:
   - Repository name: `aws-government-subsidy-assistant`
   - Description: `AWS 政府補助小助手 - 整合 Bedrock Agent 的智能問答系統`
   - 選擇 Public 或 Private
   - **不要**勾選 "Initialize this repository with a README"
4. **點擊 "Create repository"**

### 步驟 3: 推送代碼到 GitHub

複製 GitHub 提供的指令，或使用以下指令（替換為你的 GitHub 用戶名）:

```bash
# 添加遠端倉庫
git remote add origin https://github.com/你的用戶名/aws-government-subsidy-assistant.git

# 重命名分支為 main
git branch -M main

# 推送到 GitHub
git push -u origin main
```

### 步驟 4: 在 AWS Amplify 部署

#### 4.1 進入 Amplify Console

1. **登入 AWS Console**
2. **搜尋 "Amplify"**
3. **點擊 "AWS Amplify"**

#### 4.2 創建新應用

1. **點擊 "New app" → "Host web app"**
2. **選擇 "GitHub"** 作為來源
3. **點擊 "Continue"**

#### 4.3 授權 GitHub

1. **點擊 "Authorize AWS Amplify"**
2. **授權 Amplify 存取你的 GitHub**
3. **選擇你的倉庫**: `aws-government-subsidy-assistant`
4. **選擇分支**: `main`
5. **點擊 "Next"**

#### 4.4 配置建置設定

Amplify 會自動偵測到 `amplify.yml`，顯示以下內容：

```yaml
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
```

**不需要修改**，直接點擊 "Next"

#### 4.5 設定環境變數

**重要！** 在 "Advanced settings" 中添加環境變數：

1. **展開 "Advanced settings"**
2. **點擊 "Add environment variable"**
3. **添加以下變數**:
   - Key: `VITE_API_URL`
   - Value: `https://hyeavzp56b.execute-api.us-west-2.amazonaws.com/prod/chat`

#### 4.6 完成設定

1. **App name**: `aws-government-subsidy-assistant`
2. **Environment name**: `production`
3. **點擊 "Save and deploy"**

### 步驟 5: 等待部署完成

部署過程約需 3-5 分鐘，包含以下階段：

1. ⏳ **Provision** - 準備環境
2. ⏳ **Build** - 建置應用
3. ⏳ **Deploy** - 部署到 CDN
4. ✅ **完成** - 獲得網址

### 步驟 6: 取得網址並測試

部署完成後，你會獲得一個網址，格式類似：

```
https://main.d1234567890.amplifyapp.com
```

**測試步驟**:
1. 點擊網址打開應用
2. 在聊天框中輸入問題
3. 確認 Bedrock Agent 正常回應

## 🔄 後續更新流程

### 自動部署

每次推送到 GitHub 的 `main` 分支，Amplify 會自動：
1. 偵測到變更
2. 自動建置
3. 自動部署
4. 約 2-3 分鐘後生效

### 更新代碼

```bash
# 修改代碼後
git add .
git commit -m "更新描述"
git push

# Amplify 會自動部署
```

### 在 GitHub 網頁直接編輯

1. 在 GitHub 上找到要修改的文件
2. 點擊鉛筆圖示編輯
3. 提交變更
4. Amplify 自動部署

## 🎨 自訂網域（可選）

### 添加自訂網域

1. **在 Amplify Console 中**
2. **點擊左側 "Domain management"**
3. **點擊 "Add domain"**
4. **輸入你的網域名稱**
5. **按照指示設定 DNS**

## 🔧 進階設定

### 環境變數管理

在 Amplify Console 中：
1. 選擇你的應用
2. 點擊 "Environment variables"
3. 可以添加/修改/刪除環境變數
4. 修改後需要重新部署

### 分支部署

可以為不同分支創建不同的部署環境：

- `main` → Production 環境
- `dev` → Development 環境
- `staging` → Staging 環境

### 建置通知

可以設定建置完成時的通知：
1. Email 通知
2. Slack 通知
3. SNS 通知

## 📊 監控和日誌

### 查看建置日誌

1. 在 Amplify Console 中
2. 點擊應用名稱
3. 查看 "Build history"
4. 點擊任一建置查看詳細日誌

### 查看存取日誌

1. 在 Amplify Console 中
2. 點擊 "Monitoring"
3. 查看流量、錯誤率等指標

## 🛡️ 安全設定

### 設定存取控制（可選）

如果是內部工具，可以設定密碼保護：

1. 在 Amplify Console 中
2. 點擊 "Access control"
3. 啟用 "Restrict access"
4. 設定用戶名和密碼

### HTTPS

Amplify 自動提供 HTTPS，無需額外設定。

## 💰 成本估算

### Amplify 定價

- **建置時間**: 前 1000 分鐘/月免費
- **儲存空間**: 前 5GB 免費
- **資料傳輸**: 前 15GB/月免費

### 預估成本

對於這個專案：
- 建置時間: ~2 分鐘/次
- 儲存空間: ~10MB
- 每月成本: **基本免費**（在免費額度內）

## 🎯 檢查清單

部署前確認：

- [ ] 代碼已提交到 GitHub
- [ ] API URL 環境變數已設定
- [ ] amplify.yml 文件存在
- [ ] .gitignore 已設定（排除 node_modules, .env）
- [ ] package.json 中的 scripts 正確

部署後確認：

- [ ] 網站可以正常訪問
- [ ] 聊天功能正常
- [ ] Bedrock Agent 回應正常
- [ ] 沒有 Console 錯誤
- [ ] 手機版顯示正常

## 🆘 故障排除

### 建置失敗

**檢查**:
1. 查看建置日誌
2. 確認 package.json 正確
3. 確認 Node.js 版本相容

### 環境變數未生效

**解決**:
1. 確認環境變數名稱正確（`VITE_API_URL`）
2. 重新部署應用
3. 清除瀏覽器快取

### API 呼叫失敗

**檢查**:
1. 環境變數是否正確設定
2. API Gateway URL 是否正確
3. CORS 設定是否正確
4. 查看瀏覽器 Console 錯誤

## 📚 相關資源

- [AWS Amplify 文件](https://docs.aws.amazon.com/amplify/)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)
- [GitHub 文件](https://docs.github.com/)

## 🎉 完成！

部署完成後，你就有一個：
- ✅ 自動部署的網站
- ✅ HTTPS 加密
- ✅ 全球 CDN 加速
- ✅ 自動擴展
- ✅ 零維護成本

恭喜你完成部署！🚀
