# AWS 政府補助小助手

一個專為 AWS 政府補助申請而設計的互動式網頁應用程式，整合 AWS Bedrock Agent 提供智能問答服務。

## 功能特色

- 🤖 **AWS Bedrock Agent 整合**：使用 AI 即時回答補助相關問題
- 💬 **智能對話介面**：自然語言互動體驗
- 📱 **響應式設計**：支援桌面和行動裝置
- 🎨 **AWS 品牌設計**：符合 AWS 視覺識別的介面
- ☁️ **無伺服器架構**：使用 Lambda + API Gateway + Bedrock Agent

## 技術架構

### 前端
- **框架**：React 18 + TypeScript
- **建置工具**：Vite
- **樣式**：Tailwind CSS
- **圖示**：Lucide React
- **部署**：AWS Amplify

### 後端
- **AI 引擎**：AWS Bedrock Agent (Agent ID: Z56OAA2L3J)
- **API**：AWS API Gateway
- **運算**：AWS Lambda (Python 3.12)
- **架構**：無伺服器 (Serverless)

## 快速開始

### 1. 安裝依賴

```bash
npm install
```

### 2. 設定環境變數

複製 `.env.example` 為 `.env` 並填入你的 API Gateway URL：

```bash
cp .env.example .env
```

編輯 `.env` 文件：
```
VITE_API_URL=https://your-api-gateway-url.execute-api.us-west-2.amazonaws.com/prod/chat
```

### 3. 開發模式

```bash
npm run dev
```

### 4. 建置專案

```bash
npm run build
```

### 5. 預覽建置結果

```bash
npm run preview
```

## 🚀 完整部署指南

請參考 [DEPLOYMENT.md](./DEPLOYMENT.md) 了解如何：
1. 部署 Lambda 函數
2. 設定 API Gateway
3. 連接 Bedrock Agent
4. 部署到 AWS Amplify

## 專案結構

```
src/
├── components/          # React 元件
│   ├── Header.tsx      # 頁面標題
│   ├── ChatInterface.tsx # 聊天介面
│   ├── SubsidyInfo.tsx # 補助資訊
│   └── Footer.tsx      # 頁面底部
├── App.tsx             # 主要應用程式
├── main.tsx           # 應用程式入口
└── index.css          # 全域樣式
```

## 主要功能

### 1. AI 問答助手
- 即時回答補助相關問題
- 智能關鍵字識別
- 友善的對話介面

### 2. 補助資訊頁面
- 申請資格說明
- 詳細申請流程
- 所需文件清單
- 聯絡資訊

### 3. 響應式設計
- 支援各種螢幕尺寸
- 行動裝置友善介面
- 現代化的使用者體驗

## 自訂設定

### 顏色主題
在 `tailwind.config.js` 中可以調整 AWS 品牌色彩：

```javascript
colors: {
  aws: {
    orange: '#FF9900',
    blue: '#232F3E',
    light: '#F2F3F3',
  }
}
```

### AI 回應邏輯
在 `ChatInterface.tsx` 的 `generateBotResponse` 函數中可以自訂 AI 回應邏輯。

## 部署

本專案可以部署到任何支援靜態網站的平台，如：
- Vercel
- Netlify
- AWS S3 + CloudFront
- GitHub Pages

## 授權

本專案僅供學習和展示用途。